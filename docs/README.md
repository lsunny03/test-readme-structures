# Docs README

This README lives in a `docs/` subdirectory.
It should be discovered by recursive search when no root README exists.

## Purpose
Test case for PR #304 — verifying that bases with READMEs in subdirectories are handled correctly.
